{
  "name": "BCAIConfigurator",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "chmod +x ./node_modules/.bin/tsc",
    "build": "npx tsc && cp src/index.html dist/index.html && cp src/style.css dist/style.css && cp env.js dist/env.js",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}


