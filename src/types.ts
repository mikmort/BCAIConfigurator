export interface CompanyField {
  field: string;
  recommended: string;
  considerations: string;
}

export interface BasicInfo {
  companyName: string;
  industry: string;
  websiteUrl: string;
  description: string;
}
